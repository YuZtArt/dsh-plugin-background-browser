# 版本变更

## 0.3.0

- 新增人工接管与交还控制，支持点击、键盘输入、中文/密码输入、粘贴、滚动及切换标签页。
- 人工操作与模型工具串行执行，接管期间阻止模型继续操作浏览器。
- 本地登录表单验证人工登录后可与 agent 共享 Cookie。

## 0.2.2

- 导出 `./package.json`，修复 rc.2 客户端加载器回退解析路径无法发现前端模块的问题。
- 增加 manifest 及客户端导出解析测试。
- Desktop 2.0.10 侧栏入口仍待用户环境复测。

## 0.2.1

- MCP 与安装子进程显式传入 `ELECTRON_RUN_AS_NODE=1`。
- MCP 挂载到宿主 `agent.ctx` 的子生命周期，避免插件 scope 模块副本导致全局工具撞名。
- MCP client 改为匹配宿主的 peer dependency。
- 测试覆盖独立 scope 副本、多会话、子 agent、隔离和清理。

## 0.2.0

- 新增原生右侧栏只读预览：网址、标签页、截图、暂停/继续和刷新。
- 宿主管理独立 Headless Shell，MCP 通过本机 CDP 操作同一浏览器。
- 增加预览 HTTP 接口及客户端组件测试。

## 0.1.1

- 适配 DSH 0.1.5-rc.2。
- 提示词章节使用 `TOOLS_SDK - 1` 排序，避免不存在的顺序键生成 NaN。
- 在提示词组装阶段等待 MCP 就绪并重新组装，保证第一轮包含浏览器工具。

## 0.1.0

- 后台浏览器初始原型，目标为较新的 alpha API，不适用于 rc.2。
